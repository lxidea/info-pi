# Info-Pi · 1920×440 外壳设计

参数化 3D 打印外壳，承载 BOE QV101F6M-N10 屏幕 + HM-V1.0B 驱动板 + Orange Pi Zero 2W。

## 文件说明

- `enclosure.scad` — 参数化主模型（OpenSCAD）
- 输出 STL（需要 OpenSCAD 渲染）：
  - 前框 (`front`)
  - 后盖 (`back`)
  - 壁挂底板 (`wall`)

## 设计概览

整体是一块薄板，约 **280 × 80 × 15mm**：

```
正视图（屏幕朝外）：
┌──────────────────────────────────────────────────┐
│                                                  │  ← 前框 2mm
│      ┌────────────────────────────────────┐      │
│      │                                    │      │  显示窗口
│      │      显示区 261 × 61 mm            │      │  (内陷 2mm 遮挡屏幕边缘)
│      │                                    │      │
│      └────────────────────────────────────┘      │
│                                                  │
└──────────────────────────────────────────────────┘
     ← 总宽 281mm →

后视图（看后盖）：
┌──────────────────────────────────────────────────┐
│   ┌──────────┐                  ┌──────────┐     │
│   │ Orange Pi│                  │  驱动板  │     │
│   │ 65×30    │                  │  72×50   │     │
│   └──────────┘                  └──────────┘     │
│                                                  │
│           ▣ ▣                                    │  ← VESA 75×35 安装孔
│           ▣ ▣        ┌──┐                       │  ← 线缆出口
│                       └──┘                       │
└──────────────────────────────────────────────────┘

侧视图（厚度 ~15mm）：
┌────────────────┐
│ 前框 + 屏幕     │ 4+2 = 6mm
├────────────────┤
│ 后腔（驱动板/Pi）│ 7mm 
├────────────────┤
│ 后盖            │ 2mm
└────────────────┘
```

## 关键参数（在 enclosure.scad 顶部修改）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `screen_w / h / t` | 265 / 65 / 4 | 屏幕外形 |
| `board_w / h / t` | 72 / 50 / 4 | 驱动板含元器件 |
| `pi_w / h / t` | 65 / 30 / 7 | Orange Pi（含 USB-C 高度）|
| `bezel_inset` | 2 | 前框遮挡屏幕边的宽度 |
| `back_clearance` | 3 | 屏幕与后腔元器件的空气间隙 |
| `mount_spacing_x / y` | 75 / 35 | VESA 螺孔间距 |
| `board_hole_d / inset` | 2.5 / 3 | 驱动板螺丝孔径 + 距角偏移 |

## 装配方式

1. **前框** 内陷盛放屏幕（屏幕粘贴或夹紧固定）
2. **后腔** 内壁伸出柱子，驱动板（M3）和 Pi（M2.5）用螺丝固定
3. **后盖** 通过 4 颗 M3 螺丝（沿边缘）合上整体
4. **VESA 接口** 后盖中央有 4 个 M3 黄铜热熔螺母，壁挂底板拧上
5. **走线**：
   - **底部中间** 36×9mm 开口 — Type-C 电源线从这里进入外壳
   - **顶部** 8×1.5mm 扁口 — 预留触摸面板 FFC 排线（可选，默认开启）
   - **底部 Pi 端** 13×2.5mm 扁口 — microSD 卡从这里凸出（可选，默认开启）
     - **副作用**：可以在不拆机的情况下更换 SD 卡（升级系统、备份等）
     - 默认在 **底部**，若 Pi 实际安装时 SD 卡朝上请改 `sd_slot_side = "top"`

### 内部 HDMI 走线（Pi → 驱动板）

**关键设计点**：驱动板的 Mini-HDMI 在**短边**上（左短边，从屏幕背面看），不是长边。
因此 Pi 应该**竖着放**，让两块板的 HDMI 口正对面对面，直头线就能连接。

```
后视图（看进腔体内部）：

┌──────────────────────┬┬───────────────────────────────────────────┐
│                     ║║ ← 顶部触摸FFC扁口（对应驱动板顶边I2C接口）  │
│                                                                    │
│  ┌─────┐                                                          │
│  │     │ ← USB-C 电源                                              │
│  │  Pi │                                                          │
│  │ 30  │                                                          │
│  │  ×  │ ← Mini-HDMI ───── 短直头线 ───── Mini-HDMI ┐             │
│  │ 65  │                                  USB-C   │ ┌─── FPC →   │
│  │     │                                          │ │ 驱动板      │
│  │     │                                          │ │ 72×49      │
│  │     │                                          │ │            │
│  │     │ ← USB-C OTG                              │ │            │
│  └─────┘                                          └─┘            │
│                                                                    │
└───────────────────────────┬┬─────────────────────────────────────┘
                          底部 36×9mm 开口
                          （Type-C 电源线进入）
```

**布局要点**：

| 部件 | 朝向 | 备注 |
|------|------|------|
| Pi | **竖放** 长 65mm 沿 Y 轴 | 长边的接口朝右面向驱动板 |
| 驱动板 | **横放** 长 72mm 沿 X 轴 | 左短边的 HDMI/USB-C 朝左面向 Pi |
| FPC 排线 | 从驱动板右短边引出 | 绕到屏幕的右短边 |
| 触摸 FFC | 从驱动板顶边的 I2C 出 | 通过顶壁扁口出腔体 |

**HDMI 短线推荐**：

- 既然两端 HDMI 口正对，**直头 Mini-HDMI 公对公**最简单
- 长度选 **10-15cm**（够腔体内走线 + 一点弯曲冗余）
- 淘宝搜词：**"miniHDMI 公对公 直头 短线 10cm"**
- 不需要 90° 弯头，节省钱和不确定性

**电源方案**：

- 单一 Type-C 5V/2A 从底部进入
- 内部用 Type-C 一分二分线给 Pi 和驱动板各供一路

## 壁挂方案

后盖中央有 75×35mm VESA 风格的螺孔阵列（4 个 M3），通过四颗螺丝固定到壁挂底板。

**壁挂底板**特性：
- 顶部**钥匙孔槽**——只需在墙上拧一颗螺丝，把底板挂上去
- 底部**防转销孔**——附加第二颗螺丝防止外壳转动
- 4 颗 M3 沉头螺丝把后盖固定到底板上
- 拆装方便：维护时直接抬起底板从挂钩取下

## 渲染和导出

```bash
# 安装 OpenSCAD（如未安装）
sudo apt install openscad

# 命令行渲染单个部件为 STL
openscad -D 'part="front"' -o front.stl enclosure.scad
openscad -D 'part="back"' -o back.stl enclosure.scad
openscad -D 'part="wall"' -o wall.stl enclosure.scad

# 或者在 OpenSCAD GUI 里打开 enclosure.scad
# 修改 part 变量，按 F6 渲染，File→Export→STL
```

## 打印建议

- **材料**: PETG（推荐，耐热 80°C）或 PLA（70°C 上限）
- **层高**: 0.2mm
- **填充**: 20%
- **壁厚**: 文件中 `front_wall=2 / side_wall=2 / back_wall=2`
- **支撑**: 后盖的安装柱不需要支撑（自下而上打）；前框可能需要少量支撑

## 螺丝清单

| 用途 | 规格 | 数量 |
|------|------|------|
| 驱动板固定 | M3×6 自攻 | 4 |
| Orange Pi 固定 | M2.5×6 自攻 | 4 |
| 后盖合体 | M3×12 沉头 | 4-6（沿边） |
| VESA 热熔螺母 | M3 黄铜嵌件 | 4 |
| 壁挂底板连接 | M3×8 沉头 | 4 |

## 内部线缆清单

| 线缆 | 规格 | 数量 |
|------|------|------|
| Mini-HDMI 公对公直头短线 | 10-15cm（两板 HDMI 口正对） | 1 |
| Type-C 一分二充电线 | 5V/3A 共用，10cm | 1 |

## 待办（量产前需验证的尺寸）

- [ ] 驱动板螺丝孔的**实际孔径和孔距**（当前假设 M2.5、3mm 内陷）
- [ ] 屏幕背面是否完全平整（影响 `back_clearance`）
- [ ] Orange Pi 走线方向（HDMI/USB-C 的入口需要侧面预留空间）
- [ ] 触摸屏 6-pin I2C 接口（暂时不用，但布线位置可预留）

## 工作流程

1. 先用默认值 3D 打印**一个驱动板托盘**（只渲染后盖局部）验证孔位
2. 通过后再打印完整外壳
3. 装配前用游标卡尺再测一遍最终件
